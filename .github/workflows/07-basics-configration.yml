name: 07 Basics - needs

on:
  workflow_dispatch:
 

jobs:
  Build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Build project
        uses: echo "Build job running on ${{ matrix.os }}"

      