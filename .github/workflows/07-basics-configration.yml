name: 07 Basics - needs

on:
  workflow_dispatch:
 

jobs:
  Build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - name: Build project
        uses: echo "Build job running on ${{ matrix.os }}"

      