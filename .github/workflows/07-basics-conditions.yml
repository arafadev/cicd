name: 07 Basics - conditions 

on:
  pull_request:
    types:
      - closed
    
  


jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    if: ${{ github.event.pull_request.merged == true }}
    steps:
      - name: learn about conditions
        run: echo "This is step learn about conditions!"
